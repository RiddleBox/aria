# ARIA Phase 1 依赖
# 安装：pip install -r requirements.txt

# 配置解析
pyyaml>=6.0

# 语音识别（本地 Whisper）
faster-whisper>=1.0.0

# 录音
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# 截图
mss>=9.0.0

# 图像处理（截图压缩 + 视频合成）
opencv-python>=4.8.0
numpy>=1.24.0

# Replay Buffer（录屏，Phase 2）
# d3dshot 走 DXGI，游戏全屏兼容性好
# 安装：pip install d3dshot comtypes
# 注意：d3dshot 暂不支持 Python 3.12+，如遇问题改用 mss 降级方案
# d3dshot>=0.1.5

# 全局热键
keyboard>=0.13.5

# LLM（意图解析 + 视觉分析）
openai>=1.0.0

# TTS（免费，微软云）
edge-tts>=6.1.0

# 音频播放
# Windows 用系统命令播放，不需要额外库
# 如果播放有问题可以 pip install playsound==1.2.2
pydub>=0.25.1          # mp3→wav 转换（TTS播放用）

# 窗口感知（当前前台窗口 + 进程识别）
pywin32>=306           # Windows 前台窗口获取
psutil>=5.9.0          # 进程名获取
